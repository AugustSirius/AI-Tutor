# 成本验证

## 核心问题

**1M context window + 缓存，真的便宜吗？**

---

## 数学验证 (800K tokens 课程材料)

### 方案1：不使用缓存

```
每次对话：
输入：800,000 (材料) + 50 (问题) = 800,050 tokens
成本：800,050 × $0.30/1M = $0.240

5次对话总成本：$0.240 × 5 = $1.200
```

### 方案2：使用缓存

```
首次对话：
缓存写入：800,000 × $0.03/1M = $0.024
输入：50 × $0.30/1M = $0.000015
输出：300 × $2.50/1M = $0.000750
小计：$0.025

后续4次对话：
缓存读取：800,000 × $0.03/1M = $0.024 (每次)
输入：50 × $0.30/1M = $0.000015
输出：300 × $2.50/1M = $0.000750
小计：$0.025 × 4 = $0.100

5次对话总成本：$0.125
```

### 成本对比：

| 方案 | 5次对话成本 | 节省 |
|------|------------|------|
| 不使用缓存 | $1.200 | - |
| 使用缓存 | $0.125 | **90%** |

---

## 结论

**✅ 非常便宜！**

原因：
1. 缓存价格 = 正常输入 ÷ 10
2. 材料只需缓存一次，重复使用
3. Google 定价策略鼓励缓存
4. 无固定成本，按需付费

---

## vs RAG 方案

### RAG：
- 向量数据库月费：$70-200
- 架构复杂
- 可能遗漏信息

### 缓存命中方案：
- ✅ 无固定成本
- ✅ 架构极简
- ✅ 完整上下文
- ✅ 成本更低