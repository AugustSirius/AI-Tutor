# AI 课程助教系统 - 技术方案

## 🔥 一句话总结

**通过 Gemini 2.5 Flash 的 1M 上下文窗口 + 缓存机制，将完整课程材料(800K tokens)一次性加载到模型中，实现精准、低成本(90%降低)、易部署的智能助教系统，每个学生每节课仅需 ¥8.9，且可 5 分钟部署到新课程。**

---

## 🏗️ 系统架构

### 核心组件

```
┌─────────────────────────────────────────┐
│          学生提问                         │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Flask API Server                │
│  • 会话管理                              │
│  • 流式输出                              │
│  • 成本统计                              │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      Gemini 2.5 Flash                   │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│          缓存层 (800K tokens)            │
│  ┌─────────────────────────────────┐    │
│  │  system_prompt.txt              │    │
│  │  • 助教角色定义                   │    │
│  │  • 教学风格指导                   │    │
│  │  • 回答格式规范                   │    │
│  └─────────────────────────────────┘    │
│  ┌─────────────────────────────────┐    │
│  │  class_materials.txt            │    │
│  │  • 完整课程教案                   │    │
│  │  • 实验操作步骤                   │    │
│  │  • 参考文献内容                   │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

### 工作原理

**每次对话流程：**

1. **上下文构建**
   ```
   完整输入 = system_prompt + class_materials + 用户问题
   ```

2. **首次对话** (缓存写入)
   - 将 800K tokens 的教学内容写入缓存
   - 成本：$0.03/1M tokens (写入价格)
   - 缓存有效期：2小时

3. **后续对话** (缓存读取)
   - 直接读取缓存的 800K tokens 内容
   - 成本：$0.03/1M tokens (读取价格)
   - **便宜 10 倍！**（相比正常输入的 $0.30/1M）

4. **模型生成回答**
   - 基于完整上下文（800K tokens教学内容 + 用户问题）
   - 回答精准贴合课程内容
   - 自动引用相关章节

### 关键优势

**1. 精准度高**
- ✅ 每次回答都基于完整课程材料（不遗漏任何信息）
- ✅ 自动引用具体章节和页码
- ✅ 回答与课程内容一致

**2. 部署简单**
- ✅ 只需修改两个文件：
  - `system_prompt.txt` → 定义助教角色和风格
  - `class_materials.txt` → 放入新课程内容
- ✅ 无需重新训练模型
- ✅ 无需向量数据库
- ✅ 5分钟部署新课程

**3. 成本极低**
- ✅ 缓存机制降低 90% 成本
- ✅ 按需付费，无固定开销
- ✅ 每个学生每节课仅需 ¥8.9

---

## 💰 成本计算

### 使用场景：20个学生，每人每节课50次提问

#### 单个学生成本：

| 项目 | Tokens | 单价 | 成本 |
|------|--------|------|------|
| 缓存写入 (首次) | 800,000 | $0.03/1M | $0.024 |
| 缓存读取 (49次) | 39,200,000 | $0.03/1M | $1.176 |
| 问题输入 (50次) | 2,500 | $0.30/1M | $0.00075 |
| 回答输出 (50次) | 15,000 | $2.50/1M | $0.0375 |
| **单人总计** | | | **$1.238 ≈ ¥8.9** |

#### 班级成本 (20人)：

| 项目 | 单节课 | 学期(15节课) |
|------|--------|-------------|
| 总成本 | $24.76 (¥178) | $371.4 (¥2,674) |
| 人均 | $1.24 (¥8.9) | $18.6 (¥134) |

### 成本对比

#### 不使用缓存：
```
每次对话：800,000 tokens × $0.30/1M = $0.24
50次对话 × 20人 = $240/节课
学期(15课) = $3,600

使用缓存后节省：$3,228 (90%) ✅
```

#### vs 人工助教：
```
助教时薪：¥100/小时
每节课：2小时 × ¥100 = ¥200
学期(15课)：¥3,000

AI助教学期成本：¥2,674
节省：¥326 (11%) + 7×24小时在线
```

---

## 🔄 跨课程复用

### 部署新课程只需 3 步：

**1. 准备课程材料** (5分钟)
```bash
# 将新课程内容整理到一个文件
cat 课件.txt 实验手册.txt 参考文献.txt > class_materials.txt
```

**2. 定制助教风格** (5分钟)
```bash
# 编辑 system_prompt.txt
# 定义：课程名称、教学目标、回答风格
```

**3. 重启服务** (1分钟)
```bash
pkill -f api_server.py
nohup python3 api_server.py > api.log 2>&1 &
```

### 可应用课程示例：

- ✅ 质谱仪器原理（当前）
- ✅ 蛋白质组学
- ✅ 代谢组学
- ✅ 科研写作
- ✅ 实验室安全
- ✅ 任何有教材的课程

---

## 🎯 技术优势总结

### vs RAG (检索增强生成)

| 特性 | 我们的方案 | RAG方案 |
|------|-----------|---------|
| 架构复杂度 | ⭐ 极简 | ⭐⭐⭐⭐ 复杂 |
| 固定成本 | $0 | $70-200/月 (向量DB) |
| 回答质量 | ⭐⭐⭐⭐⭐ 完整上下文 | ⭐⭐⭐ 可能遗漏 |
| 部署时间 | 5分钟 | 数天 |
| 维护成本 | 几乎为0 | 需专人维护 |

### 核心创新点

1. **充分利用大上下文窗口**
   - 1M tokens = 可容纳完整教材
   - 无需复杂的检索系统
   - 回答基于完整知识

2. **缓存机制降低成本**
   - 课程材料只需加载一次
   - 后续读取成本降低 90%
   - 适合高频问答场景

3. **极简部署架构**
   - 两个文本文件定义整个系统
   - 切换课程只需替换文件
   - 无需技术背景即可操作

---

## 📈 预期效果

### 教学效果
- ✅ 学生随时获得即时答疑（不受助教时间限制）
- ✅ 回答精准贴合课程内容（基于完整教材）
- ✅ 支持连续对话（记忆上下文）
- ✅ 24/7 在线，响应时间 < 3秒

### 助教减负
- ✅ 处理 80% 重复性问题
- ✅ 助教专注于复杂问题和实验指导
- ✅ 自动记录常见问题，优化教学

### 成本效益
- ✅ 人均学期成本 ¥150
- ✅ 可复用到多门课程
- ✅ 无固定开销，按需付费